IntegrationMetadata:
  type: object
  required:
    - user_id
    - provider
    - created_at
  properties:
    user_id:
      type: string
      format: uuid
    provider:
      $ref: "#/components/schemas/IntegrationProvider"
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
      nullable: true
