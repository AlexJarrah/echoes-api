LibraryAddAlbumTrack:
  allOf:
    - $ref: "#/components/schemas/AlbumTrack"
    - type: object
      required:
        - album_reference_id
        - track_reference_id
      properties:
        album_reference_id:
          type: integer
          format: int64
        track_reference_id:
          type: integer
          format: int64
