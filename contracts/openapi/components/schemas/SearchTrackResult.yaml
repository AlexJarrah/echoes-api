SearchTrackResult:
  allOf:
    - $ref: "#/components/schemas/Track"
    - type: object
      required:
        - confidence
      properties:
        confidence:
          type: integer
          format: uint8
          description: Confidence score (0-100)
          minimum: 0
          maximum: 100
