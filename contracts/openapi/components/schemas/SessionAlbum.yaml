SessionAlbum:
  type: object
  required:
    - id
    - name
  properties:
    id:
      type: string
      format: uuid
      description: Album UUID.
    name:
      type: string
      description: Album name.
    asset_url:
      type: string
      description: URL to the album cover asset.
