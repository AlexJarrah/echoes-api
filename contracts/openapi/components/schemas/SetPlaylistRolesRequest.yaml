SetPlaylistRolesRequest:
  type: array
  items:
    type: object
    required:
      - user_id
      - role
    properties:
      user_id:
        type: string
        format: uuid
      role:
        $ref: "#/components/schemas/PlaylistRoleType"
