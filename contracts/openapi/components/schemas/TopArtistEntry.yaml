TopArtistEntry:
  type: object
  required:
    - id
    - rank
    - artists
    - current
  properties:
    id:
      type: string
      format: uuid
    rank:
      type: integer
      format: uint
    artist:
      $ref: "#/components/schemas/Artist"
    current:
      $ref: "#/components/schemas/EntityPlays"
    previous:
      $ref: "#/components/schemas/EntityPlays"
    change:
      $ref: "#/components/schemas/EntityChange"
