TopEntityChange:
  type: object
  required:
    - play_count
    - duration
  description: Values change in percentage.
  properties:
    play_count:
      type: integer
      format: int64
    duration:
      type: integer
      format: int64
      description: Play duration in seconds
