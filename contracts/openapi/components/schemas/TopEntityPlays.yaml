TopEntityPlays:
  type: object
  required:
    - play_count
    - duration
  properties:
    play_count:
      type: integer
      format: uint64
    duration:
      type: integer
      format: uint64
      description: Play duration in seconds
