TrackPlayStats:
  type: object
  required:
    - track
    - play_count
    - play_duration
  properties:
    track:
      $ref: "#/components/schemas/Track"
    play_count:
      type: integer
      format: uint64
    play_duration:
      type: integer
      format: uint64
